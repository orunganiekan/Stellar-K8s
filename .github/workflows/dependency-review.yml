name: Dependency & License Review

# Automatically detects stale dependencies, license drift, and security
# advisories on every push/PR that touches Cargo files, and on a weekly
# schedule so that newly published advisories are caught promptly.
#
# Addresses GitHub Issue #1119.

on:
  push:
    branches: [main]
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "deny.toml"
      - ".cargo/audit.toml"
  pull_request:
    branches: [main]
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "deny.toml"
      - ".cargo/audit.toml"
  schedule:
    # Run weekly on Mondays at 08:00 UTC to catch newly published advisories.
    - cron: "0 8 * * 1"
  workflow_dispatch:
    inputs:
      full_check:
        description: "Run full check (including unmaintained + license)"
        type: boolean
        default: true

permissions:
  contents: read
  security-events: write  # for uploading SARIF results

concurrency:
  group: dep-review-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  # ── 1. cargo audit — known vulnerabilities ──────────────────────────────────
  cargo-audit:
    name: Cargo Audit (Vulnerabilities)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key: "dep-audit"
          extra-tools: "cargo-audit"
          cache-key: "ci-audit"

      - name: Install cargo-audit
        run: |
          for i in 1 2 3; do
            cargo install --locked cargo-audit && break
            echo "Attempt $i failed, retrying..."
            sleep 5
          done

      - name: Run cargo audit
        run: |
          echo "── Security advisory audit ──────────────────────────────────────"
          cargo audit \
            --ignore RUSTSEC-2020-0071 \
            --ignore RUSTSEC-2024-0380 \
            --ignore RUSTSEC-2024-0381 \
            --ignore RUSTSEC-2026-0085 \
            --ignore RUSTSEC-2026-0086 \
            --ignore RUSTSEC-2026-0087 \
            --ignore RUSTSEC-2026-0091 \
            --ignore RUSTSEC-2026-0094 \
            --ignore RUSTSEC-2026-0097 \
            --ignore RUSTSEC-2026-0098 \
            --ignore RUSTSEC-2026-0099 \
            --ignore RUSTSEC-2026-0104 \
            --ignore RUSTSEC-2026-0149 \
            --ignore RUSTSEC-2026-0187 \
            --ignore RUSTSEC-2026-0204

      - name: Generate audit JSON report
        if: always()
        run: |
          cargo audit --json 2>/dev/null > audit-report.json || true
          echo "Audit report saved to audit-report.json"

      - name: Upload audit report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cargo-audit-report
          path: audit-report.json
          retention-days: 30

  # ── 2. cargo deny — license + bans + advisories ─────────────────────────────
  cargo-deny:
    name: Cargo Deny (Licenses + Bans)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key: "dep-deny"
          extra-tools: "cargo-deny"
      - name: Install cargo-deny
        run: |
          for i in 1 2 3; do
            cargo install --locked cargo-deny && break
            echo "Attempt $i failed, retrying..."
            sleep 5
          done

      - name: Check licenses
        run: |
          echo "── License compliance check ─────────────────────────────────────"
          cargo deny check licenses

      - name: Check bans (forbidden + duplicate crates)
        run: |
          echo "── Dependency bans check ────────────────────────────────────────"
          cargo deny check bans

      - name: Check advisories via deny
        run: |
          echo "── Advisory check via cargo-deny ────────────────────────────────"
          cargo deny check advisories

      - name: Check sources (registry whitelist)
        run: |
          echo "── Source registry check ────────────────────────────────────────"
          cargo deny check sources

  # ── 3. Stale dependency detection ───────────────────────────────────────────
  stale-deps:
    name: Stale Dependency Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # Run on schedule and manual dispatch; also on dep changes
    if: >
      github.event_name == 'schedule' ||
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key: "dep-stale"
          extra-tools: "cargo-outdated"
          cache-key: "ci-stale"

      - name: Install cargo-outdated
        run: |
          for i in 1 2 3; do
            cargo install --locked cargo-outdated && break
            echo "Attempt $i failed, retrying..."
            sleep 5
          done

      - name: Check for outdated dependencies
        id: outdated
        run: |
          echo "── Outdated dependency report ───────────────────────────────────"
          # Generate report; exit 0 even if outdated crates exist (advisory only)
          cargo outdated --workspace --depth 1 2>&1 | tee outdated-report.txt
          OUTDATED_COUNT=$(grep -c "^[[:space:]]*[a-z]" outdated-report.txt || true)
          echo "outdated_count=${OUTDATED_COUNT}" >> "$GITHUB_OUTPUT"

      - name: Upload outdated report
        uses: actions/upload-artifact@v4
        with:
          name: outdated-dependencies-report
          path: outdated-report.txt
          retention-days: 30

      - name: Annotate PR with outdated count
        if: github.event_name == 'pull_request' && steps.outdated.outputs.outdated_count > 0
        run: |
          echo "::warning::${{ steps.outdated.outputs.outdated_count }} outdated direct dependencies found. Review outdated-dependencies-report artifact."

  # ── 4. Dependency diff on PRs ────────────────────────────────────────────────
  dep-diff:
    name: Dependency Diff (PR only)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key: "dep-diff"
          extra-tools: "cargo-lock"
          cache-key: "ci-diff"

      - name: Install cargo-diff-tree
        run: |
          for i in 1 2 3; do
            cargo install --locked cargo-lock && break
            echo "Attempt $i failed, retrying..."
            sleep 5
          done

      - name: Summarise Cargo.lock changes
        run: |
          echo "── Cargo.lock dependency diff ───────────────────────────────────"
          git diff origin/${{ github.base_ref }}..HEAD -- Cargo.lock | \
            grep -E "^[+-]name|^[+-]version" | \
            sed 's/^+/ADDED:   /; s/^-/REMOVED: /' || echo "No Cargo.lock changes detected"

  # ── 5. License drift report (scheduled) ─────────────────────────────────────
  license-report:
    name: License Drift Report
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key: "dep-license"
          extra-tools: "cargo-license"
          cache-key: "ci-license"

      - name: Install cargo-license
        run: |
          for i in 1 2 3; do
            cargo install --locked cargo-license && break
            echo "Attempt $i failed, retrying..."
            sleep 5
          done

      - name: Generate full license inventory
        run: |
          echo "── Full license inventory ───────────────────────────────────────"
          cargo license --all-features --tsv 2>/dev/null | tee license-inventory.tsv
          echo ""
          echo "── License summary ──────────────────────────────────────────────"
          cargo license --all-features 2>/dev/null | sort | uniq -c | sort -rn | head -20

      - name: Upload license inventory
        uses: actions/upload-artifact@v4
        with:
          name: license-inventory
          path: license-inventory.tsv
          retention-days: 90
